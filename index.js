document.getElementById('readNum').onclick = convertToNumWords(number) {
    const numLength = number.Length()
    console.log(`okok`)
}

let num = document.getElementById('readNum')
convertToNumWords(num)